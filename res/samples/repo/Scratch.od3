// record creation and selection within a component
module UnitsChaining {
    // record creation
    val rec = { i : 1 + 1, j : 2, k : 3 }
  
    
    component f (x) => z where {
      val z = rec#j + rec#k
    }
    
    // ? constrain the poly func    
    val w = f(rec)

}

