// record creation and selection
module Record2 {
    // record creation
    val rec = { i : 1 + 1, j : 2, k : 3 }
    
    // direct record selection
    // val x = rec#i + rec#j
    // infered record selection
    
    component f x => () where {
      val y = x#i + x#j
    }
    
    // constrain the poly func    
    val z = f(rec)
}

