module Test {

    component inc(x) {
        return (x + 1)
    }

    val X = inc(3)

    //val Y = X

    //val Z = Y + X + 23 

    //val W = inc(5) + 2

    // what the fuck!
    // val Z = Y + X + Z

}

